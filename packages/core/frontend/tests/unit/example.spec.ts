// import { shallowMount } from "@vue/test-utils";
// import HelloWorld from "@/components/HelloWorld.vue";

// TODO: Remove. We can't actually enable the default test since it's not valid,
// but I still want it around as an example. And I don't care to write a test that will be
// invalidated as soon as anyone does literally anything
describe("Testing", () => {
  it("runs", () => {
    expect(true).toBe(true);
  });
});

/*
describe("HelloWorld.vue", () => {
  it("renders props.msg when passed", () => {
    const msg = "new message";
    const wrapper = shallowMount(HelloWorld, {
      propsData: { msg },
    });
    expect(wrapper.text()).toMatch(msg);
  });
});
*/
